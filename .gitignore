# ── Build outputs ──────────────────────────────────────────────────────────────
dist/
tmp/
out-tsc/
*.tsbuildinfo
.expo
.expo-shared

# ── Dependencies ───────────────────────────────────────────────────────────────
node_modules/

# ── SECRETS — never commit these ───────────────────────────────────────────────
.env
.env.*
!.env.example

# WhatsApp auth sessions (contain private encryption keys)
session/
sessions/

# Runtime state (chatbot enabled JIDs, etc.)
data/

# ── Logs ───────────────────────────────────────────────────────────────────────
*.log
npm-debug.log
yarn-error.log

# ── Replit internals ───────────────────────────────────────────────────────────
.cache/
.local/
.replit

# ── OS / IDE ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# ── Source maps (large, not needed in repo) ───────────────────────────────────
*.map

# ── Misc ───────────────────────────────────────────────────────────────────────
/.sass-cache
/connect.lock
/coverage
/typings
testem.log
